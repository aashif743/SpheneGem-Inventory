# Dependencies
node_modules/
**/node_modules/

# Database backups / dumps — contain live client data, NEVER commit
*.sql.gz
/*.sql
backup_*.sql
u*_gem_inventory.sql
# (migrations are intentionally tracked — they live in backend/migrations/)
!backend/migrations/*.sql

# Environment variables — NEVER commit real .env files
backend/.env
.env.local
.env.*.local

# Generated files (created at runtime)
backend/src/invoices/*.pdf
backend/src/uploads/

# Mac junk
**/._.DS_Store
**/.DS_Store
frontend/._build
**/._ *

# Build output
frontend/build/
frontend/build.zip

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Capacitor (mobile build artifacts)
frontend/android/
frontend/ios/
